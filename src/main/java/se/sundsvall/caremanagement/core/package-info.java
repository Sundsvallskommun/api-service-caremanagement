/**
 * Core module — errand envelope, lifecycle, type registry, events.
 *
 * Public API: {@code core.api.*}.
 */
@ApplicationModule(displayName = "Core")
package se.sundsvall.caremanagement.core;

import org.springframework.modulith.ApplicationModule;
