/**
 * What other modules may call on the event log directly: recording the Lifecare reads and writes careM itself makes on
 * an errand's behalf.
 */
@NamedInterface("spi")
package se.sundsvall.caremanagement.eventlog.spi;

import org.springframework.modulith.NamedInterface;
