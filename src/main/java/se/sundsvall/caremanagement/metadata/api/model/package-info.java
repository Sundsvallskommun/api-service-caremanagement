/**
 * Exposed so consuming modules can read the {@link Lookup} values returned by {@code MetadataService}.
 */
@NamedInterface("model")
package se.sundsvall.caremanagement.metadata.api.model;

import org.springframework.modulith.NamedInterface;
