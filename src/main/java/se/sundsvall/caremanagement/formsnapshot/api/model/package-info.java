/**
 * Form-snapshot API model.
 *
 * <p>
 * Exposed so type modules can return the {@link se.sundsvall.caremanagement.formsnapshot.api.model.FormSnapshot}
 * envelope from their read endpoints (and reference it when capturing), through the exposed form-snapshot service.
 * </p>
 */
@NamedInterface("api")
package se.sundsvall.caremanagement.formsnapshot.api.model;

import org.springframework.modulith.NamedInterface;
