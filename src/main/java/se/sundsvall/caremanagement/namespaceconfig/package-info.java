/**
 * Namespace configuration module — per-namespace settings (display name, notification
 * config, etc.). Read by other modules when they need to resolve namespace-scoped behavior.
 */
@ApplicationModule(displayName = "Namespace Config")
package se.sundsvall.caremanagement.namespaceconfig;

import org.springframework.modulith.ApplicationModule;
